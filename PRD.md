# DocMind OpenViking 제거 및 클라우드 원본 동기화 PRD

작성일: 2026-09-21 · 버전: 0.17 · 상태: 이전 대화 30일 조회·검색 및 만료 처리 확정 / 기능 구현·검증 전

## 1. 확정된 목적과 구조

All-in-One DC 5.0 사용자가 클라우드 드라이브에 파일을 올리면, 클라우드 서버의 실제 저장 폴더에 암호화된 원본이 저장된다. DocMind는 이 서버 원본과 색인을 동기화한다. 변경된 파일만 Windows의 uEncryptor2로 임시 복호화하고 파싱·청킹·색인한 뒤, 생성한 평문 파일을 삭제한다. 검색은 저장된 청크와 벡터를 사용하며 질의마다 원본을 복호화하지 않는다.

```text
사용자: All-in-One DC 5.0의 [home]:\ 등에 업로드·수정·삭제
  ↓
클라우드 서버: 등록된 실제 저장 루트 3개에 암호화 원본 저장
  ↓  변경 감지 + 매일 00:00 Asia/Seoul 전체 정합성 확인
완료된 원본 버전 확인 → 작업 등록
  ↓
Windows에서 uEncryptor2.exe "암호화 원본" "작업별 임시 출력" /dec
  ↓
결과 검증 → DocMind 파싱 → 청킹 → embedding → 색인 검증
  ↓
최신 버전 활성화 → 생성된 평문 임시 파일 삭제 → 처리 완료

DocMind 질문 + 전체/폴더/선택 문서 범위
  → BM25 128 + dense 128 → RRF → 최대 128청크
  → 청크별 앞 2,400자 → Jina reranker v3.5 API → Top 5
  → 기존 답변 생성 경로에서 근거와 함께 응답
```

사용자에게 DocMind용 재업로드나 별도 수동 복호화를 요구하는 흐름은 이번 목표가 아니다. v0.5~v0.6의 수동 수집 제안과 현 CLI 워커 일괄 제외는 최신 설명으로 대체한다. 이전 문서는 이력으로 보존한다.

## 2. 원본 경로와 소유 관계

원본의 권위는 클라우드 서버의 실제 암호화 저장 폴더다. 사용자 PC의 드라이브 표시는 해당 저장 공간을 노출하는 논리 경로이며 별도 복제 원본으로 취급하지 않는다.

| source | 서버의 실제 저장 루트 | 사용자가 보는 경로 | 상태 |
|---|---|---|---|
| 1 | `D:\UPLEXSOFT\UDRIVE\USER\TEST1` | 해당 사용자의 `[home]:\` | 사용자 설명 및 IMG_4424 사진으로 확인 |
| 2 | `D:\UPLEXSOFT\UDRIVE\DEPT\_DEPT_2` | `[DEPT_2]:\` | IMG_4424 사진으로 확인 |
| 3 | `D:\UPLEXSOFT\UDRIVE\DEPT\_DEPT_1` | 미확인 | 서버 경로는 사진으로 확인, 사용자 표시명은 사진에 없음 |

사진의 원화 모양 경로 구분자는 Windows 경로의 역슬래시로 표기했다. 탐색기에 `test1 (U:)`도 보이지만 드라이브 문자 U를 전 사용자 공통 고정 매핑으로 가정하지 않는다. `_DEPT_1`의 표시명을 `[DEPT_1]:\`로 추정해 확정하지 않는다.

각 루트 아래의 상대 경로를 유지해 논리 경로를 만든다. 예를 들어 source 2의 `D:\UPLEXSOFT\UDRIVE\DEPT\_DEPT_2\부서자료\문서.pptx`는 `[DEPT_2]:\부서자료\문서.pptx`로 표시한다. 이것은 표시 경로 매핑이며 실제 클릭 가능한 링크 프로토콜이 확인됐다는 뜻은 아니다. 세 루트는 source ID로 구분해 동일 상대 경로의 문서를 혼합하지 않는다.

초기 구현 범위는 등록된 세 source 전체로 확정한다. 사용자·부서별 범위를 세분화하고 축소하는 것은 후속 단계로 둔다. 초기 공용 범위에서도 로그인과 등록 source 경계는 유지하며, 임의 서버 파일 접근까지 허용하지 않는다. 향후 범위 축소를 위해 source/소유 관계 metadata와 scope 필터 구조는 유지한다.

- 실제 루트와 사용자 표시 경로, 소유자/접근권한을 별도 metadata로 보존한다. 같은 `[home]:\` 이름을 가진 다른 사용자의 저장 공간을 합치지 않는다.
- 폴더 이름과 상대 경로는 원본 구조를 그대로 표시한다. DocMind의 문서/폴더 쓰기 API는 원본 시스템 작업과 연동하고, DocMind DB의 경로만 바꿔 원본과 불일치시키지 않는다. 실제 원본 작업 방식이 검증되기 전에는 쓰기 작업을 성공한 것으로 응답하지 않는다.
- 물리 파일명/경로가 사용자에게 보이는 이름과 다를 경우, 확인된 제품 metadata 매핑을 사용한다. 아직 확인되지 않은 이름 규칙을 추측하지 않는다.
- 초기에는 사용자가 정한 세 source 전체의 공용 범위를 적용한다. 사용자별·부서별 제한을 도입할 때는 클라우드 계정/ACL 매핑을 검증하며, OS 접근 가능 여부로 최종 사용자 권한을 추정하지 않는다.

## 3. 역할과 배치

| 구성요소 | 책임 |
|---|---|
| All-in-One DC 5.0 | 사용자 업로드, 암호화 원본 저장 및 원본 측 권한 관리 |
| Windows 수집·복호화 실행부 | 원본 감지/목록 확인, 버전 식별, CLI 실행, 임시 출력 검증·전달·삭제 |
| DocMind Docker | 파싱, 청킹, embedding, 색인, 활성 버전과 작업 상태 관리, 검색·답변 |
| 검색 저장소 | 청크 본문, 벡터, 문서/폴더/버전/권한 metadata와 근거 위치 보관 |

기존 검색 저장소(Elasticsearch의 키워드·벡터 검색)를 우선 재사용한다. 사용자가 말한 vectordb를 별도 신규 벡터 제품 도입 요구로 해석하지 않는다. Windows 실행부는 기존 EXE를 호출하며 복호화 알고리즘을 재구현하지 않는다.

배치는 Windows 장비 쪽 Docker로 확정한다. DocMind는 Docker에서 실행하고 uEncryptor2 실행부는 Windows 호스트에서 동작한다. Linux 컨테이너에서 Windows EXE를 직접 실행하지 않는다. 대상 Windows의 Docker 실행 환경·자원·호스트/컨테이너 연결 방식은 구현 시 확인하고 요청 인증·결과 수신·임시파일 수명 관리를 연결한다. 기존 Linux 서버와의 망 간 연결 해결을 새 배치의 선행 조건으로 두지 않는다.

별도 메시지 브로커를 선행 도입하지 않는다. 기존 작업 저장소/큐를 재사용할 수 있는지 확인하고, 부족하면 DB 기반 작업 claim/lease를 추가한다. Windows에서 DocMind 작업을 가져오거나 결과를 보내는 통신 형태는 실제 배치와 연결 가능 경로에 맞춰 정한다.

## 4. CLI 실행에 대한 근거와 검증 범위

사용자 제공 실행 사진에서 다음이 관찰됐다.

- `uEncryptor2.exe "원본" "출력" /dec`와 복호화 모드 플래그, DOC/PPTX 경로의 `[SUCCESS]` 표시.
- 기존 출력 파일이 있는 한 사례에서는 덮어쓰기 질문에 `y` 입력.
- 사진 범위에서 OTP 발급·입력 단계는 보이지 않음.
- 마지막 요약은 `/dec` 실행에서도 `Encryption Success` 문자열을 사용함. 로그 단어 하나만으로 성공을 판정하면 안 됨.

정적 조사에서 발견된 웹 OTP 안내를 매 실행의 필수 절차라고 단정하지 않는다. 실행 사진도 로그아웃·재부팅 후 서비스 계정 실행의 증거로 확대하지 않는다. 사용자 전달의 공급사 ‘서버 무인 연동 미지원’ 답변은 제품의 공식 지원 범위에 대한 정보로 유지하고, 현재 환경에서 CLI 호출이 성공하는 기술적 관찰과 구분한다.

이번 요구사항은 이 CLI를 사용한 자동 수집 흐름이다. 구현 전 아래 항목을 작은 비민감 표본으로 실제 검증한다. 동일한 정적 조사나 공급사 질의를 반복하는 것을 선행 조건으로 삼지 않는다.

1. 사진의 실행 파일과 조사 대상 파일의 버전·해시, 실행 계정과 의존 모듈을 대조한다.
2. 작업별 새 출력 경로를 써서 덮어쓰기 확인 없이 실행 가능한지 확인한다. `/y` 등 확인되지 않은 옵션에 의존하지 않는다.
3. 출력 파일의 실제 읽기 가능성, 종료 코드, 성공/손상/암호화되지 않은 입력의 결과를 확인한다. 출력 존재만으로 성공 처리하지 않는다.
4. 의도한 작업 계정과 로그인/로그아웃·재부팅 상태에서 동작하는지 확인한다. 로그인이 필요하면 운영 의존성으로 기록하고 요구사항 충족 여부를 결정한다.
5. 키 입력이나 대화창이 나타나면 자동 처리 가능으로 간주하지 않고 명확히 중단한다. 검증 우회는 하지 않는다.
6. 기존 제품 로그, 임시 폴더, stdout/stderr에 키나 파일 본문이 남는지 확인한다. 시험 결과에는 비밀값을 포함하지 않는다.

단일 실행 mutex 단서가 있어 최초 CLI 동시성은 호스트당 1로 한다. 실제 제약을 확인하기 전 병렬 실행을 늘리지 않는다. 이 문서 작성에서는 CLI 실행 시험이나 운영 변경을 수행하지 않았다.

## 5. 변경 감지와 원본 일관성

### 즉시 감지

- 등록 루트와 하위 폴더의 생성·수정·이동·삭제를 관찰해 작업 후보를 만든다. 이벤트는 중복/누락될 수 있으므로 그 자체를 유일한 동기화 근거로 삼지 않는다.
- 파일 생성 이벤트 직후 복호화하지 않는다. 공식 업로드 완료 신호가 있으면 사용하고, 없으면 크기/수정 시각 안정 구간, 읽기 가능 여부와 해시 전후 확인을 조합한다. 안정 구간만으로 쓰기 완료가 보장된다고 판단하지 않는다.
- 원본을 덮어쓰거나 이동하지 않는다. 일관된 입력이 필요하면 암호화 상태의 작업별 복사본을 사용하고 복사 전후 원본 fingerprint를 대조한다.
- 색인 활성화 직전에 원본 버전과 삭제 여부를 재확인한다. 처리 중 바뀌거나 삭제된 결과는 활성화하지 않고 폐기/재처리한다.

### 일일 정합성 확인

- 매일 00:00 Asia/Seoul에 세 루트를 source별로 전수 확인해 누락 이벤트, 미처리 변경, 삭제, 실패 작업을 복구한다.
- 스캔은 scan ID와 source 범위를 갖는다. 루트 연결·접근 권한·전체 순회 완료가 확인된 스캔에서만 누락을 삭제로 확정한다.
- 접근 거부, 일시 연결 실패, 일부 하위 폴더 열람 실패를 대량 삭제로 처리하지 않는다. 해당 source 스캔은 실패로 기록하고 다음 시도에서 재개/재확인한다.
- 이벤트로 잡힌 삭제도 실제 부재와 원본 접근 상태를 확인한 뒤 적용한다. 잠깐 사라졌다 다시 생기는 파일 교체와 구분한다.
- 여기서 동기화 대상은 서버에 실제 저장된 원본이다. 서버에 업로드되지 않은 클라이언트 PC 변경까지 감지하는 기능은 아니다.

## 6. 문서 ID, 버전, 작업 상태

기존 File/File2Document/Document와 작업 체계를 우선 재사용하고 schema를 확인한 뒤 필요한 필드만 추가한다.

| 개념 | 필요한 정보 |
|---|---|
| source | 실제 루트, 표시 경로 매핑, 소유권, 마지막 성공 스캔 |
| folder | 안정 ID, source, parent, 상대 경로 |
| document | 안정 ID, 원본 식별자(검증된 경우), folder, 활성 version |
| version | 암호화 원본 fingerprint, 검증된 내용 hash, 상태, chunk set |
| job | 문서/버전/단계별 idempotency key, claim/lease, attempt, 오류, 정리 상태 |
| deletion | 삭제 확인 시각, 검색 제외 시각, 30일 보관 종료 시각 |

- 경로·ciphertext hash는 변경 감지 근거이며 문서 영구 ID 자체로 쓰지 않는다. 재암호화만으로 ciphertext가 달라질 수 있으므로 필요시 복호화 내용 hash로 재색인을 줄인다.
- 제품 내부 DOCID/DOCVIDX/DOCDELIDX는 영속성·재사용·삭제 연결 계약이 미확인이다. 이름만 보고 안정 ID로 채택하지 않는다.
- 원본의 검증된 ID로 이동을 연결할 수 있으면 문서 ID를 유지한다. 증거가 없으면 삭제+신규로 처리하고 hash만으로 다른 사용자의 문서를 합치지 않는다.
- 새 버전은 별도로 색인한 뒤 전체 완료를 확인해 활성화한다. 문서별 구/신 청크가 검색에서 섞이지 않게 한다.
- 일반 수정 처리 실패 시 이전 검색 가능 버전은 유지하면서 동기화 실패/지연을 표시한다. 원본 삭제·권한 회수는 이전 버전도 즉시 검색 제외한다.
- lease 만료 후 이전 작업의 완료 통보가 최신 버전을 덮거나 삭제 문서를 되살리지 않도록 세대 또는 fencing token을 검증한다.

정상 흐름:

`DISCOVERED → WAITING_SOURCE_STABLE → DECRYPTING → PARSING → INDEXING → CLEANUP → COMPLETE`

`INDEXING`에서 실제 청크·벡터·버전과 원본 상태를 확인한 뒤 검색 가능 버전을 활성화한다. 검색 활성화와 임시 파일 정리 완료를 각각 기록한다. 정리 실패 상태는 `CLEANUP_FAILED`로 명시해 정상 전체 완료로 숨기지 않는다.

오류 흐름에서는 실행이 끝나거나 중단된 모든 경우에 정리 단계를 거친다. `RETRY_WAIT`, `FAILED`, `DELETED`, 대화형 입력이 실제 요구된 경우의 `ACTION_REQUIRED`를 구분한다. 재시도 시 원본에서 다시 복호화하며 이전 평문을 무기한 보관하지 않는다.

## 7. 평문 임시 파일 삭제 계약

사용자가 요구한 ‘복호화된 파일이 서버에 남으면 안 됨’은 처리에 필요한 잠깐의 임시 파일 외에는 평문 원본/파생 파일을 계속 보관하지 않는다는 요구로 반영한다. 검색용 청크·벡터는 사용자가 설명한대로 검색 저장소에 남는다.

- Windows 복호화 출력은 작업별 제한된 임시 폴더에 생성한다. 암호화 원본 파일과 다른 경로를 사용한다.
- Docker로 전달하는 경우 Docker 업로드 저장소, 파서 작업 폴더, 변환된 PDF/이미지/OCR 임시 파일까지 평문 흔적 범위에 포함한다. Windows 출력 하나만 삭제하는 것으로 완료하지 않는다.
- 새 임시 파일에는 파일 생성 시점부터 접근 제한을 적용한다. 일반 파일 보관/백업/프리뷰 캐시에 복사하지 않는다.
- 성공 시 색인 완료와 전달 수신 확인 후 즉시 임시 파일을 정리한다. 실패·timeout·중단 시에도 finally 정리하고 재시도에는 암호화 원본을 다시 사용한다.
- 강제 종료로 finally가 실행되지 않은 경우 시작 시 잔여 작업을 검사하고 짧은 주기의 청소 작업으로 복구한다. 실행 중 작업을 삭제하지 않도록 lease와 정리 소유권을 확인한다.
- 삭제 실패는 경로·작업 ID만으로 기록하고 운영자에게 표시하며 재시도한다. 잔여 평문을 정리 완료로 기록하지 않는다.
- 파일 삭제는 저장매체의 복구 불가능한 소거를 보증하지 않는다. 작업 공간 암호화와 백업 제외 등을 포함해 보관 정책을 구현한다.
- Jina API로 전달하는 질문과 청크 텍스트는 기존 최종 검색 결정에 따른다. 로그에 문서 본문이나 키를 덤프하지 않는다.

기존 파이프라인이 원본 평문을 객체 저장소에 계속 보관하거나 프리뷰 파일을 유지하는지 구현 전에 확인하고 이 수집 경로에서는 제거한다. 검색 결과는 보관된 청크와 위치 metadata를 사용한다. 원문 열기는 사용자 클라우드의 논리 경로로 연결하는 안을 우선 검토하며, 별도 평문 원본 보관을 전제하는 미리보기 기능은 넣지 않는다. 링크 지원 여부는 실제 클라이언트 환경에서 확인한다.

### 7.1 MinIO와 객체 저장소 처리

사용자 확인 사항: Windows 임시 출력뿐 아니라 MinIO에도 복호화된 파일이 계속 저장되면 안 된다. 이 클라우드 동기화 경로는 원본 문서의 영구 저장소로 MinIO를 사용하지 않는다. 암호화 원본의 권위 저장소는 All-in-One DC 서버의 세 루트이고, DocMind는 검색용 청크·벡터·metadata를 보관한다. 저장소 자체 암호화를 추가하더라도 복호화 원본의 별도 사본을 계속 보관하는 것으로 이 원칙을 대체하지 않는다.

복구 스냅샷의 소스를 읽어 다음 의존성을 확인했다. 실행 중 환경의 저장소 내용/암호화 활성 상태를 조사하거나 데이터를 삭제한 것은 아니다.

- `api/db/services/file_service.py:655`: 업로드 파일 바이트를 `STORAGE_IMPL.put`으로 저장.
- 같은 파일 `:657–661`: 썸네일 생성 및 저장.
- `rag/svr/task_executor.py:312–313`: 객체 저장소에서 원본을 읽어 청킹을 시작.
- 같은 파일 `:404–429`: 청크 이미지가 있으면 객체 저장소에 저장하는 경로 존재.

따라서 기존 업로드 경로를 그대로 재사용한 뒤 Windows 출력만 삭제하는 구현은 불충분하다. 신규 source의 파서 입력을 작업 수명에 묶인 임시 파일/바이너리로 공급하고, 원본·썸네일·청크 이미지의 영구 저장을 막는 경로를 추가한다. 기존 chunker는 가능한 범위에서 재사용한다. 모든 병렬 파싱 작업이 입력 소비를 완료하기 전에 임시 입력을 삭제하지 않는다.

파생 이미지가 필요한 분석은 작업 중에 수행하고 검색에 필요한 텍스트·근거 위치를 남긴 뒤 이미지는 정리한다. 존재하지 않는 MinIO 이미지/원문을 가리키는 참조를 결과에 남기지 않는다. 원본 미리보기·재파싱·다운로드는 기존 영구 객체 존재를 전제로 동작하지 않도록 수정한다. 재색인 시에는 서버의 암호화 원본을 다시 복호화한다.

청크 화면의 표시 경로도 구분한다. 백업 코드에서 청크 본문은 Elasticsearch의 `content_with_weight` 등 색인 필드로 조회하고, 청크 이미지는 `image_id`로 `/documents/images/<id>` API를 호출해 객체 저장소에서 읽는다. 원문 PDF/Office 미리보기 역시 별도 파일 읽기 경로다. 따라서 MinIO 원본/이미지를 삭제해도 색인의 청크 텍스트 표시 자체는 유지할 수 있지만, 기존 이미지·원문 미리보기는 그대로 유지되지 않는다. 청크 표시 기능 전체를 제거하는 것으로 해석하지 않는다. 현재 계획은 청크 텍스트·표의 텍스트/HTML·근거 위치 표시를 유지하고 원문 열기를 클라우드 경로로 연결하는 것이다. 시각적 청크 이미지까지 계속 제공해야 한다면 보관/재생성 방식을 별도로 결정해야 하며, 이 요구 확인을 이미지 보관 허용으로 간주하지 않는다.

MinIO 서비스 자체의 제거 여부는 다른 기능의 의존성을 조사한 뒤 결정한다. 이번 결정으로 기존 버킷을 일괄 삭제하지 않는다. 기존 원본·썸네일·이미지를 정리할 때는 source/문서/버전별 소유 관계와 참조를 확인하고, 객체 버전 관리·복제·백업에 사본이 남는지도 확인한다. 평문 비보관 정책과 충돌하는 새 평문 백업을 만들지 않으며, 기존 데이터의 정책에 맞는 이행·복구 방법은 실제 변경 전에 정한다.

### 7.1.1 청크 이미지 처리 제안 — 확정 전

2026-09-21 검토안: 본문·표는 색인의 청크 텍스트/HTML로 표시하고, 도표·그림 등 텍스트만으로 내용을 전달하기 어려운 부분의 이미지에 한해 암호화된 파생물 보관을 허용하는 방안을 제안한다. 기존 청커·embedding·C 검색 설정은 변경하지 않는다. 기존 파서의 이미지 영역과 청크 연결 정보를 재사용할 수 있는지 먼저 검증하며, 자동 판별의 정확도를 가정하지 않는다.

이 안은 원본 PDF/Office/HWP 전체, 모든 페이지 캡처, 일반 본문 캡처를 MinIO에 중복 저장하는 안이 아니다. 다만 선택한 청크 이미지도 원문 일부의 사본이다. '원본 파일 비보관 + 필요한 파생 이미지의 암호화 보관'으로 정책 범위를 별도로 정해야 하며, 암호화 자체가 보관 허가를 대신하지 않는다. 확정 전에는 기존 제7.1절의 이미지 비보관 기준을 유지한다.

채택할 경우의 요구사항:

- 원본 전체는 클라우드에만 보관한다. 임시 복호화 원본·페이지 렌더·이미지 중간 파일은 처리 완료/실패/중단 시 정리한다.
- MinIO/S3 호환 저장소는 선택된 파생 이미지 전용으로 제한한다. 저장소와 분리된 키 관리로 암호화하고, 실제 운영 이미지 반입 전에 암호화·복원·키 권한을 검증한다. 구현체 지원 방식은 별도 확인한다.
- 이미지 ID는 tenant/source/document/version/chunk 소유 관계에 연결한다. 인증된 API가 매 조회마다 현재 원본 문서 권한과 활성 버전을 검사한 후 TLS로 제공한다. 공개 버킷·권한을 우회하는 영구 URL을 사용하지 않는다.
- 서버의 불필요한 평문 디스크 캐시를 만들지 않으며 응답은 no-store 정책을 적용한다. 사용자가 보는 순간 브라우저에는 복호화된 이미지가 전달되므로 암호화 저장이 화면 캡처나 사용자 저장까지 방지한다고 설명하지 않는다.
- 원본 삭제·권한 회수 시 이미지 조회도 즉시 차단한다. 원본 갱신 후 새 버전으로 전환하고 구버전 이미지와 고아 객체를 정리한다. 이미지의 물리 보존기간은 별도로 확정하며, 삭제 청크 30일 보관 정책을 자동 적용하지 않는다. 객체 버전·복제·백업도 정리 대상에 포함한다.
- 실제 표본으로 본문/표 가독성, 그림의 청크 연결, 다른 사용자 접근 차단, 갱신/삭제 후 참조 정리, 임시 평문 잔여, 저장량을 검증한다. 기존 문서에 이미지가 없으면 텍스트 표시로 처리하고, 무조건 원본 재복호화를 유발하지 않는다.

파생 이미지 보관을 허용하지 않는 경우에는 텍스트/표/근거 위치만 표시하고 원문은 클라우드에서 연다. 조회마다 복호화·렌더링하는 방식은 실행 조건과 지연이 미검증이므로 기본안으로 채택하지 않는다.

### 7.1.2 원문·청크 좌우 비교 화면 — 공급 방식 미확정

2026-09-21 사용자가 제공한 화면은 왼쪽 원문 전체 미리보기와 오른쪽 청크 목록을 함께 제공한다. 왼쪽은 일부 청크 그림만 저장해서 대체할 수 있는 화면이 아니다. 이 화면을 유지하는 데 필요한 원문 공급 방식을 다음과 같이 검토한다. 현재 단계에서 화면 폐지 또는 영구 미리보기 사본 보관이 결정된 것은 아니다.

- 오른쪽 청크 본문·표는 색인 데이터로 표시할 수 있다.
- 왼쪽 원문은 사용 권한이 확인된 문서/PDF/페이지 렌더 공급 경로가 필요하다. 외부 클라우드 앱으로 이동하는 링크는 앱 내부의 좌우 비교 화면과 동등하지 않다.
- 병행 비교: All-in-One의 지원되는 인증 문서 스트림 또는 임베드 가능한 미리보기 경로. API 존재, 세션/권한 전달, 브라우저 임베드 허용, PDF 범위 요청, Office/HWP 변환 지원은 미확인이다. 서버 저장소의 암호화 파일 경로만으로 브라우저 미리보기를 제공할 수 있다고 가정하지 않는다.
- 구현 중 검증 대상으로 선정: 검색 결과 클릭 시 서버에서 임시 복호화·렌더링해 인증 API로 전달하고 세션 종료/TTL/오류 시 정리. 구체적인 검증 과제는 제7.1.3절을 따른다. 현재 도구의 무인 호출 조건·공식 지원 제약 및 응답 지연을 먼저 검증해야 하며, 이미 가능한 연동으로 취급하지 않는다. 평문 임시파일 삭제와 복수 요청 중 입력 수명 관리가 필요하다.
- 별도 정책 대안: 원문 또는 전체 미리보기 파생물을 암호화해 객체 저장소에 보관. 빠른 재조회는 가능하나 문서 전체 내용의 중복 사본이 생기므로 현재 비보관 정책에 자동 포함하지 않는다. 일부 그림 저장 제안과 구분하고 별도 결정한다.

청크 선택 시 페이지 이동/강조까지 유지하려면 document ID·원본 버전·페이지·좌표 매핑을 보존해야 한다. 현재 클라우드 최신본과 색인한 버전이 다를 때 같은 문서처럼 강조하지 않고 버전 불일치를 표시한다. 실제 파서·형식별 좌표 제공과 렌더링 일치 여부를 검증한다. 화면 구성은 가능하지만 원문 공급 경로 검증 전 동일 기능의 제공 완료를 약속하지 않는다.

### 7.1.3 구현 중 검증 과제: 검색 결과 클릭 시 임시 복호화 미리보기

2026-09-21 사용자 결정: 검색 결과를 클릭할 때 원본을 임시 복호화해 좌측 원문 미리보기로 보여주는 방식의 가능 여부를 구현 과정에서 확인한다. 검증을 구현 과제에 포함하기로 확정한 것이며, 무인 실행 가능 또는 운영 채택이 확정된 것은 아니다. 공급사의 서버 무인 자동 복호화 미지원 답변은 그대로 기록하고, 기술적 실행 결과와 공식 지원 여부를 분리해 판단한다.

목표 흐름: 청크/벡터 검색과 답변·근거 목록은 먼저 표시한다. 사용자가 원문 확인을 선택하면 문서 ID·버전과 사용자 권한을 검증하고, Windows 작업별 임시 공간에서 해당 원본만 복호화한다. 필요한 형식 변환 후 인증된 미리보기 API로 전달하여 원문·청크를 나란히 보여준다. 미리보기 대기·실패가 검색과 답변 완료를 막지 않게 한다. 검색만 한 경우에는 미리보기용 복호화를 실행하지 않는다.

검증 항목:

1. 비민감 표본으로 기존 uEncryptor2의 실행 계정·로그인/로그아웃·uDrive 세션·키 입력·대화 프롬프트 필요 여부, 종료 코드·결과 파일·실패/timeout 판정과 반복 실행을 확인한다. 자동 실행이 불가하면 그 조건과 근거를 남기고, 인증/정책 우회나 입력 자동화가 해결책이라고 가정하지 않는다.
2. Windows 실행부와 Docker/API 사이 요청·상태·결과 전달 경로를 검증한다. 실제 서버 경로를 브라우저 입력으로 받지 않고 등록된 source/document ID로 해석한다. 미리보기 생성과 각 파일/페이지 요청에 현재 권한·삭제 상태·문서 버전을 확인한다.
3. PDF와 실제 사용 Office/HWP 표본에서 첫 화면 표시, 페이지 이동, 필요 시 범위 요청, 청크 페이지/좌표 강조를 검증한다. 색인 버전과 원본 버전이 다르면 불일치를 표시하고 잘못된 강조를 하지 않는다.
4. 최초/반복 조회의 대기 시간, 복호화·전달·변환 시간, 동시 요청 시 자원 사용과 색인 작업 간섭을 측정한다. 허용 동시성·timeout·임시파일 TTL을 실측으로 정한다. 측정 전 응답 속도를 보장하지 않는다.
5. 같은 문서의 중복 요청, 사용자 취소·연결 끊김, 복호화/변환 실패, 프로세스·서버 재시작을 검증한다. 작업 디렉터리는 격리하고 진행 중인 다른 요청의 파일을 조기 삭제하지 않는다. 종료 이벤트를 놓쳐도 서버 측 TTL/회수 작업으로 정리한다.
6. Windows/Docker의 원본 임시파일·변환 PDF·페이지 이미지·전달용 임시파일을 정리하고, MinIO 영구 원본·미리보기 객체와 불필요한 평문 캐시를 만들지 않는지 검사한다. 삭제 실패는 표시·재시도한다. 브라우저 응답은 인증/TLS 및 no-store를 적용하되 사용자 화면 캡처까지 막는다고 설명하지 않는다.

완료 산출물: 재현 가능한 실행 조건, 표본별 성공/실패와 시간, 중단 후 잔여 파일 검사, 권한/버전 검증 결과를 남기고 '가능 / 특정 조건에서 가능 / 현재 방식으로 불가'로 판정한다. 검증을 통과하면 클릭 기반 임시 미리보기를 연결한다. 실패하면 검색·청크 표시를 유지하면서 원문 미리보기 불가 사유를 표시하고, 클라우드 제공 미리보기 등 대안을 검토한다. MinIO 영구 사본 저장으로 자동 전환하지 않는다.

### 7.2 MySQL에 남는 정보

복구 스냅샷의 `api/db/db_models.py`를 기준으로 문서/폴더/작업의 관리 정보가 SQL에 저장되는 구조를 확인했다. 현재 운영 DB의 실제 행이나 저장된 본문을 조회한 것은 아니다.

| 범위 | 기존 코드에서 확인된 정보 | 새 설계에서의 역할 |
|---|---|---|
| File / File2Document | 파일·폴더 ID, parent, tenant, 이름, location, 문서 연결 | 원본 source/폴더 구조와 문서 연결 |
| Document | 이름, 형식, 크기, content_hash, 청크/토큰 수, active_chunk_set_id, 진행 상태 | 변경 감지와 활성 검색 버전 관리 |
| Task / ParserRun | 대상 문서, 페이지 범위, 상태, 재시도, chunk IDs, 파서 버전, 오류, 보관 시각 | 작업 중복 방지, 재처리와 성공 판정 |
| 사용자/KB/프로젝트/설정 | 소유 관계, 접근 설정, 파서·모델 설정 | 인증·범위와 검색 설정 관리 |
| 새 동기화 관리 정보 | 기존 모델에 모두 구현된 것은 아님 | source 매핑, 원본 마지막 확인, scan ID, 임시 파일 정리 상태, 삭제 확인/보관 기한 |

SQL의 location이나 raw_artifact_ref는 파일 자체가 아니라 참조다. 기존 MinIO 위치를 삭제한 뒤 여전히 유효한 원본인 것처럼 사용하지 않고 source 참조/임시 작업 참조를 구분한다. 복호화 원본 바이너리와 전체 추출 본문을 신규 SQL 필드에 복사하지 않는다. 검색용 청크 본문·벡터는 검색 색인에 둔다.

다만 현재 SQL 구조를 ‘문서 내용이 전혀 남지 않는 metadata 전용 DB’라고 설명해서는 안 된다. 다음 내용 저장 가능 지점이 있다.

- `Conversation.message`와 `Conversation.reference`: 사용자 결정으로 질문·LLM 답변·인용 근거를 30일 동안 보관하고 이전 대화 조회·검색을 제공한다. 인용에 사용한 문서 ID/버전·청크 ID·페이지/위치·표시 경로와 실제 인용한 최소 텍스트를 저장하며, 전체 원본이나 검색 후보 128개 전체를 대화 기록에 복사하지 않는다. 기간 계산·권한·만료 처리는 제9.4절을 따른다.
- `Document.thumbnail`: 객체 참조 외에 base64 이미지 저장을 표현할 수 있는 필드다. 신규 클라우드 경로에서는 썸네일 이미지를 SQL로 우회 보관하지 않는다.
- OpenViking 관련 digest `output_json`, 폴더 `l0_text/l1_text`, catalog `snapshot_json`: 문서 유래 요약이 남을 수 있는 기존 구조다. 자동 생성 경로 제거와 기존 기록의 보관/정리를 구분해 조사한다.
- 진행/오류 메시지와 parser warnings: 원문이나 키를 실수로 담지 않도록 생성 지점을 점검한다.

MySQL 자체는 문서 동기화·권한·작업 상태에 필요하다. DB를 없애거나 기존 데이터를 일괄 삭제하는 결정은 아니다. SQL 백업도 실제로 포함된 대화·참조·요약의 보관 정책을 따라야 한다.

### 7.3 DB 암호화의 적용 순서

요구사항: MySQL과 검색 저장소 등 지속 저장되는 데이터도 암호화한다. 구현 순서는 기능 검증 후 저장 암호화를 적용할 수 있으나, 이를 운영 실데이터의 무기한 평문 보관 허용으로 해석하지 않는다.

1. 격리된 환경의 비민감/합성 문서로 수집→복호화→색인→임시 파일 삭제→검색→원본 동기화를 검증한다. 평문 임시 파일 정리, 권한 검사, 인증·비밀값 비기록은 이 단계부터 구현한다.
2. 운영 배치의 실제 OS, DB 종류·버전, Docker 영속 볼륨 경로와 복구 방식을 확인해 저장 암호화를 적용한다. MySQL/검색 색인 외에 작업·대화 기록, DB 로그, 임시 볼륨, 스냅샷·백업·복제본도 범위에 포함한다. 전송 암호화(TLS)는 저장 암호화와 별도로 적용한다.
3. 기존에 작성된 시험 데이터·백업의 암호화 여부도 확인한다. 새 데이터만 암호화하는 설정으로 이전 사본까지 보호된다고 간주하지 않는다. 필요하면 암호화된 저장소에 시험 데이터를 재구축하고 이전 사본을 정책대로 정리한다.
4. 키를 DB 데이터·이미지·소스와 분리해 관리하고, 재시작·키 접근 장애·백업 복원·검색 품질/속도를 검증한 뒤 운영 실문서를 수집한다.

검색 가능한 청크/벡터를 애플리케이션에서 불투명 암호문으로 바꾼 채 기존 BM25/dense에 넣는 방식은 채택하지 않는다. 저장 계층의 암호화로 검색 처리 시 엔진이 원래 데이터를 읽게 하는 방식을 우선 검토한다. 저장 암호화는 정당한 DB 조회 권한으로 읽는 데이터를 숨겨주지 않으므로 사용자 접근 제어를 대신하지 않는다.

제품별 지원은 현재 설치 버전을 확인한 뒤 결정한다. MySQL InnoDB는 tablespace 및 일부 로그의 저장 암호화를 제공하고 키 관리가 필요하다. 자체 운영 Elasticsearch는 호스트의 디스크/파일시스템 암호화가 공식적인 적용 경로다. 원본 평문 비보관 요구는 DB/볼륨 암호화를 적용해도 유지한다.

공식 참고: [MySQL InnoDB 저장 암호화](https://dev.mysql.com/doc/refman/8.0/en/innodb-data-encryption.html), [Elastic 자체 운영 보안](https://www.elastic.co/docs/deploy-manage/security).

## 8. 삭제와 보관

- 원본 삭제가 확정되면 해당 문서의 모든 활성 검색 청크를 제외한다. 지연된 색인 작업이 이를 되살리지 못하게 한다.
- 기존 사용자 결정대로 삭제된 문서 metadata와 청크는 검색 비활성 상태로 30일 보관한 뒤 정리한다. 정상 검색으로 반환하지 않는다.
- 30일 보관은 복호화 원본 파일의 보관 허용이 아니다. 복호화 임시 파일은 성공·실패 모두 처리 직후 정리한다.
- 원본 클라우드 파일 자체의 삭제/백업 정책은 All-in-One DC 측에 남는다. DocMind가 클라우드 암호화 원본을 삭제하지 않는다.
- 내부 삭제 상태나 휴지통 이동으로 원본이 물리적으로 남는 경우에는 해당 상태/폴더의 의미를 확인해 source 규칙에 반영한다. 단순 물리 파일 존재만으로 사용자에게 보이는 활성 문서라 단정하지 않는다.

## 9. 탐색·검색 사용자 기능

- 세 source의 권한 있는 폴더와 문서를 원래 상대 경로로 탐색한다. 이름/경로 찾기와 본문 검색을 구분한다.
- 파일별 동기화 상태, 검색 가능 버전, 마지막 원본 확인, 갱신 실패/삭제를 보여준다. 처리가 끝나면 카탈로그 발행 없이 검색된다.
- 전체: 현재 프로젝트에서 사용자가 접근 가능한 모든 검색 가능 문서.
- 폴더: 선택 폴더와 모든 하위 폴더의 문서 합집합.
- 문서: 명시적으로 선택한 문서 합집합.
- 세 모드는 상호 배타적이다. 빈 명시 선택, 권한 없는 선택, 삭제된 선택은 범위를 확대하지 않고 적절한 오류/재선택으로 처리한다. 유효한 빈 폴더는 0건이다.
- 권한 검증을 트리·목록·양쪽 검색 lane·결과·원문 연결 모두에 적용한다. 서버 OS 접근권한과 최종 사용자 문서 권한은 구분한다.
- OpenViking의 자동 폴더/문서 선택, 폴더별 후보 quota, AI 폴더 요약/자동 정리, 카탈로그 생성·발행은 이번 제품 경로에 없다. AI 정리는 후속 단계다.

검색 API 초안:

```json
{"question":"질문","project_id":"project-id","scope":{"mode":"folders","folder_ids":["folder-id"]}}
```

`documents` 모드는 `document_ids`, `all` 모드는 선택 배열 없이 사용한다. tenant와 권한 주체는 서버 인증에서 결정한다. mode 혼용/알 수 없는 값은 거부한다.

### 9.1 문서 ID와 경로 매핑

사용자 요구: 사용자가 보는 문서 경로와 실제 서버 경로를 매핑하고 문서 생성·삭제·수정·복사·이동 API를 제공한다. 파일 작업은 원본 시스템과 검색 색인 모두에 일관되게 반영되어야 한다.

문서는 `document_id`로 식별하고 경로는 변경 가능한 속성으로 둔다. 다음 매핑을 SQL에서 관리한다.

| 항목 | 의미 / 예시 |
|---|---|
| document_id | DocMind의 안정 문서 ID |
| source_id | TEST1 / DEPT_2 / DEPT_1 저장 공간을 구분하는 내부 ID |
| folder_id, relative_path | source 아래 폴더와 `자료/보고서.pdf` 같은 상대 경로 |
| user_visible_path | 예: `[DEPT_2]:\자료\보고서.pdf` |
| server_path | 등록된 source root + 검증된 상대 경로, 서버 내부 사용 전용 |
| source_object_id | 원본 제품의 영구 ID 계약이 검증되는 경우에만 연결 |
| source_version, active_index_version | 실제 원본 버전과 현재 검색 가능한 버전 |

동일 상대 경로라도 source나 소유자가 다르면 다른 문서다. `[home]`은 로그인 사용자별 매핑으로 해석하고 전역 공용 경로로 취급하지 않는다. 클라이언트 요청은 문서/폴더/source ID와 파일명으로 받고 임의의 물리 절대 경로를 받아 실행하지 않는다. 일반 사용자 응답에는 표시 경로를 제공하며 서버 물리 경로는 노출하지 않는다.

서버 경로를 계산할 때 경로 이탈, 절대 경로/UNC 주입, junction·symlink를 통한 source 밖 접근, Windows 대소문자·Unicode 정규화·예약 이름·ADS 문제를 검사한다. 등록 루트 내부라는 문자열 prefix 비교만으로 안전성을 판정하지 않는다. 원본 제품이 다른 이름 매핑을 사용한다면 그 계약을 우선한다.

### 9.2 파일 작업 API 초안

| 작업 | API 초안 | 원본 및 색인 처리 |
|---|---|---|
| 문서 조회 | `GET /api/v1/docmind/documents/{id}` | 표시 경로, 버전, 동기화 상태와 허용 작업 반환 |
| 폴더 목록 | `GET /api/v1/docmind/folders/{id}/children` | 권한 있는 하위 폴더·문서 반환 |
| 문서 생성/업로드 | `POST /api/v1/docmind/documents` | 대상 source/folder에 원본 생성, 새 ID 발급, 색인 작업 |
| 내용 수정 | `PUT /api/v1/docmind/documents/{id}/content` | 같은 문서의 새 원본 버전 작성, 새 색인 검증 후 전환 |
| 이름 변경 | `PATCH /api/v1/docmind/documents/{id}` | 원본 이름 및 경로 매핑 갱신 |
| 삭제 | `DELETE /api/v1/docmind/documents/{id}` | 원본 시스템의 삭제/휴지통 처리 확인 후 검색 제외 |
| 복사 | `POST /api/v1/docmind/documents/{id}/copy` | 대상 폴더에 별도 원본 생성, 새 문서 ID, 권한/색인 연결 |
| 이동 | `POST /api/v1/docmind/documents/{id}/move` | 원본 이동 확인 후 경로·권한·색인 metadata 갱신 |
| 작업 조회 | `GET /api/v1/docmind/operations/{operation_id}` | 원본 작업 완료와 색인 동기화 완료를 각각 반환 |

폴더 생성·이름 변경·이동·삭제도 같은 source 작업 어댑터를 사용하도록 설계한다. 폴더 삭제는 기본적으로 비어 있는 폴더만 허용하고 재귀 삭제는 별도 명시 작업으로 구분한다. 본문 편집 UI는 현재 요구에 포함하지 않으며 내용 수정 API는 파일 교체 업로드를 의미한다.

- 생성/복사/이동은 대상 folder ID를 받는다. 이름 충돌은 기본적으로 오류이며 덮어쓰기를 묵시적으로 수행하지 않는다.
- 수정/이름 변경/삭제/이동에는 기대 원본 버전 또는 ETag를 사용한다. 다른 클라우드 사용자가 먼저 변경하면 충돌로 반환하고 최신 변경을 덮지 않는다.
- 작업 요청의 멱등 키를 보관해 timeout 후 재시도가 중복 생성·복사를 만들지 않게 한다. 비동기 작업은 `202 + operation_id`를 반환하고, 접수만으로 원본·색인 완료를 의미하지 않는다.
- 응답 상태는 최소 원본 작업 상태와 색인 동기화 상태를 구분한다. 원본 생성/갱신 후 색인 실패는 원본 실패처럼 표시하지 않고 재색인 가능 상태로 남긴다.
- 원본 작업 성공을 확인한 뒤 SQL 매핑과 색인 작업을 내구성 있는 기록으로 연결한다. 원본 변경 직후 프로세스가 중단되면 작업 이력과 정합성 스캔으로 복구한다. DB와 파일시스템/외부 API가 하나의 원자적 transaction이라고 가정하지 않는다.
- API가 만든 파일 이벤트도 동일 멱등 처리 경로로 수렴시켜 이벤트 감시와 API 완료 처리의 중복을 막는다.
- 동일 source의 이동은 안정 문서 ID를 유지한다. 다른 source로 이동은 실제 제품의 ID·권한·암호화 정책을 확인한 뒤 정의한다. 복사 성공 전에 원본을 삭제하는 방식은 금지한다.
- 원본 경로/이름만 바뀌어도 embedding 입력에 파일명이 포함되는 설정이면 재임베딩이 필요할 수 있다. 청크 본문이 같다는 이유만으로 무조건 재색인을 생략하지 않는다.
- 복사본은 독립된 문서 ID와 대상 권한을 갖는다. 본문 hash가 같더라도 검색/권한 관계를 합치지 않는다. 데이터 재사용은 활성 버전·권한·필드 일치가 검증된 경우에만 최적화한다.
- 삭제 성공 시 지연된 파싱 작업이 문서를 되살리지 못하게 한다. DocMind metadata/청크 30일 보관은 원본 제품의 복원 가능 기간을 보장하지 않는다.

### 9.3 원본 시스템 쓰기 경계

API를 설계하는 것과 All-in-One DC 저장 폴더에 직접 파일을 쓰는 것은 다르다. 기존 조사에서 제품 내부 문서/버전/삭제 이력과 ACL이 발견됐으므로, 직접 NTFS 작업만으로 제품 상태·암호화 정책이 일관되게 갱신되는지는 미확인이다. 새 평문 파일을 `D:\UPLEXSOFT\UDRIVE\...`에 직접 저장하는 구현은 채택하지 않는다.

All-in-One DC의 지원 API/SDK 또는 검증된 클라이언트 경로를 원본 작업 어댑터로 사용한다. 복호화 무인 연동 미지원 답변만으로 일반 파일 작업 API도 미지원이라고 단정하지 않는다. 실제 생성·수정·삭제·복사·이동 인터페이스와 사용 권한 전달 방식은 아직 확인되지 않았다.

확인되지 않은 기능은 capabilities에 미지원으로 표시하고 `SOURCE_OPERATION_UNSUPPORTED`처럼 명확히 실패시킨다. 원본은 그대로인데 MySQL과 색인만 수정해 성공한 것처럼 보이게 하지 않는다. 실제 쓰기 연계가 확보되기 전에도 ID/경로 매핑·조회·검색·원본 변경 감지는 구현할 수 있다.

신규 파일 업로드는 공식 경로를 통해 암호화된 원본으로 저장되는지 확인해야 한다. 전송·처리에 필요한 임시 평문은 기존 삭제 계약을 따르며 MinIO의 영구 원본으로 저장하지 않는다. 권한 검증은 원본 읽기·쓰기·삭제 및 대상 폴더 쓰기를 각각 확인한다. 부서 공간에서 개인 공간으로의 복사 같은 작업은 해당 원본 제품 정책을 따른다.

### 9.4 이전 대화 조회·검색 — 30일 보관

2026-09-21 사용자 결정: 질문·LLM 답변·인용 근거를 기록으로 남겨 30일 동안 다시 찾아볼 수 있게 한다. LLM 답변 생성과 별개인 대화 이력 기능이며, 화면과 API 구현 및 검증은 아직 수행하지 않았다.

- 로그인한 사용자는 본인의 최근 30일 대화 목록을 날짜순으로 보고, 질문·답변의 키워드로 찾아 상세 내용을 다시 열 수 있다. 문서 본문을 찾는 구성 C 검색과 대화 이력 검색은 별도다.
- 초기 문서 검색 대상이 세 source 전체인 것과 대화 공개 범위는 다르다. 대화는 작성자 계정/tenant 소유로 관리하고 다른 사용자의 대화를 기본 공개하지 않는다. 공용 서비스 계정을 모든 사용자의 대화 소유자로 기록하지 않는다.
- 보관 기산점은 각 질문이 등록된 시각이며 해당 질문·답변 쌍과 참조를 묶어 30일 후 만료한다. 재조회나 같은 대화에 새 질문을 추가해도 기존 질문의 보관 기한을 연장하지 않는다. 새 질문은 별도의 30일 기한을 갖는다. 서버 UTC 시각으로 계산하고 화면에는 사용자 시간대를 적용한다.
- 목록·검색·상세·후속 질문 문맥 구성에서 만료 기록을 즉시 제외한다. 정기 정리 작업은 만료 질문/답변/참조와 부속 대화 검색 자료를 삭제하며, 기록이 모두 만료된 빈 대화 껍데기도 정리한다. 작업 지연이 조회 기간 연장으로 이어지지 않게 조회 조건에 만료 시각을 적용한다.
- 원본 수정·삭제 후에도 남아 있는 대화는 답변 당시의 기록임을 표시한다. 인용 링크는 조회 시점의 문서 존재·권한·버전을 확인하며, 삭제되거나 접근할 수 없는 원문을 열거나 임시 복호화하지 않는다. 기존 답변을 현재 문서의 최신 사실로 표시하지 않는다. 사용자별 문서 권한 제한을 도입할 때는 회수된 자료가 포함된 인용·답변의 재노출 차단도 검증한다.
- 저장 대상은 질문·답변·실제 인용에 필요한 최소 정보다. 원본 파일·미리보기 이미지의 영구 보관을 이 기능으로 허용하지 않는다. DB 및 백업은 제7.3절 암호화 기준을 따른다.
- 30일 이후 서비스에서 조회할 수 없어야 한다. 백업의 보관/회수 일정은 운영 백업 정책에서 확정하고, 복원한 데이터에도 만료 정리를 적용해 지난 대화가 다시 노출되지 않게 한다. 온라인 DB 삭제와 백업의 즉시 물리 삭제를 동일하게 보장하지 않는다.

수용 기준: 새로고침/재로그인 후 본인 대화를 조회하고 키워드로 찾을 수 있다. 다른 사용자/tenant 기록을 조회할 수 없고, 만료 경계 직후 목록·검색·직접 상세 API·후속 질문 문맥에서 제외된다. 같은 대화의 신규 질문으로 이전 기록이 연장되지 않는다. 정리 실패/재시작/백업 복원 후에도 만료 기록이 재노출되지 않으며, 삭제 원본의 링크는 접근 불가로 표시한다.

## 10. 구성 C 최종 검색 계약

C는 BM25와 dense를 독립 실행하고 RRF로 합친 뒤 Jina API로 재순위화하는 방식이다. dense는 키워드 일치를 요구하거나 BM25 부족 시에만 실행하는 보충 검색이 아니다. 기존 tokenizer/query 생성기와 BGE-M3 embedding을 유지한다.

| 설정 | 최종 값 |
|---|---|
| BM25 / dense lane | 각 최대 128청크 |
| dense k / num_candidates / similarity | 2048 / 4096 / 0.0 |
| 병합 | 동일 가중치 RRF `1/(60+rank)`, rank constant 60 |
| 후보 제한 | 문서당 최대 8청크, 전체 최대 128청크 |
| rerank 입력 | 각 청크 앞 최대 2,400자, `window_mode=prefix` |
| reranker | hosted Jina API `jina-reranker-v3.5` |
| 최종 결과 | 최대 5청크 |
| 질문 분해 | `atomic_decomposition=false` |

- tenant·범위·권한·활성 버전 조건은 두 lane 후보 생성 전에 동일하게 적용한다. 전역 Top K 뒤 필터링하지 않는다.
- 평가 코드 `remote_adapters.py`의 C 분기와 `evaluator.py:rrf`를 기준으로 이식한다. RRF 동점은 chunk ID 오름차순, API 입력은 후보 chunk ID 순서, 최종 동점은 Jina 점수→RRF 점수 내림차순→chunk ID 오름차순이다.
- 2,400자는 리랭커 입력의 청크별 상한이다. 저장 청크 길이·토큰 수·전체 API 요청 길이가 아니다. 후보 수와 청크 길이는 임의로 바꾸지 않는다.
- 공식 provider/secret 경로로 모델 ID를 명시한다. 평가용 credential override를 운영에 복사하거나 실패 시 v3로 몰래 바꾸지 않는다.
- 실제 API 입력의 토큰/자동 절단을 확인한다. 동일 후보·순서·입력으로 v3.5의 근거 포함률, 시간, usage 토큰을 기록한다. 구매 단가·소모량 확인 없이 비용 동일을 보장하지 않는다.

과거 r4의 C128+1,800자와 E3의 C128+2,400자 실험은 모두 v3 결과다. r4는 31문항, Top-5 evidence group 17/46 및 전체 근거 충족 5/31이었다. E3는 선택한 4문항의 동일 128개 후보에서 전체 근거 충족 0/4→2/4, evidence group 1/5→3/5였다. 이 결과를 v3.5 검증 또는 전체 답변 정확도로 표시하지 않는다. v3.5 회귀와 운영 적용은 아직 수행하지 않았다.

## 11. 구현 순서와 변경 영역

1. 복구 기준 확인 후 격리 checkout에서 OpenViking 의존·자동 route·폴더 quota·publish UI를 제거하고 공통 scope/C 검색을 구현한다. 기존 색인 문서로 검증한다.
2. 세 원본 루트와 사용자 경로·권한·활성/삭제 상태 규칙을 매핑한다. CLI의 실제 무인 실행 조건은 비민감 표본으로 검증한다. 이 단계에서 제7.1.3절의 검색 결과 클릭 기반 임시 복호화 미리보기를 시험 구현하고 가능 조건·표시 지연·평문 정리 결과를 기록한다.
3. 변경 감지·안정된 원본 버전 확인·복호화 실행·결과 전달·실패 포함 평문 정리를 연결한다. 기존 객체 저장소/파서에 평문이 남는 지점을 조사한다.
4. 초기 전수 스캔 및 변경/삭제/재시도/자정 정합성 확인을 구현한다. 기존 문서와 새 source의 ID 매핑을 dry-run해 중복·누락을 확인한다. 문서 생성·수정·삭제·복사·이동 API를 원본 작업 어댑터와 연결하고 원본 작업 완료와 색인 완료를 구분한다.
5. 별도 DB/index에서 기능 회귀·복구를 검증한 뒤 저장/전송 암호화와 키 관리·암호화 백업 복원을 검증한다. 운영 데이터/이미지 복구 수단을 확보하고 제한된 범위부터 전환한다.
6. 제품 코드의 OpenViking 호출 0을 확인한 뒤 제품용 서비스를 중지하고 배포 의존성을 정리한다. 평가용 인스턴스·봉인 실험·복구용 영속 자료는 별도 보존한다.

주요 코드 영역은 `docmind_api_service.py`, `restful_apis/docmind_api.py`, `docmind_hierarchy_service.py`, `docmind_registration_service.py`, `docmind_document_path_service.py`, generation/publish/catalog 서비스, 프런트 DocMind 화면 및 Compose/환경설정이다. 운영 컨테이너에는 코드가 이미지에 포함되므로 checkout 수정만으로 적용된 것으로 보지 않는다.

## 12. 수용 기준

| 시나리오 | 기대 결과 |
|---|---|
| 클라우드 새 파일 | 완료된 암호화 원본 감지→복호화→색인→검색 가능→모든 임시 평문 정리 |
| 원본 업로드/수정 중 | 불완전 버전으로 색인 성공 처리하지 않음 |
| 같은 파일 갱신 | 새 버전 검증 후 전환, 구/신 청크 혼합 없음 |
| 처리 중 원본 수정/삭제 | 오래된 결과의 활성화/삭제 문서 부활 없음 |
| 중복 이벤트·재시도 | 중복 문서·청크 없음, 임시 출력 무기한 보관 없음 |
| CLI 오류·대화창·timeout | 실패 구분, 원본 유지, 임시 평문 정리, 무한 대기 방지 |
| Windows/Docker 중단 | 재시작 시 잔여 파일 정리와 작업 복구, 오래된 완료 통보 거부 |
| 원본 삭제/권한 회수 | 검색에서 제외, 삭제 청크 30일 비활성 보관 |
| 일부 루트 스캔 실패 | 해당 루트의 대량 삭제 판정 없음 |
| 폴더·문서 선택/권한 | 범위 밖·다른 사용자 문서 노출 없음 |
| 경로 매핑 | 세 source와 사용자별 home 분리, 이름/이동 후 표시 경로 갱신, 임의 물리 경로 접근 차단 |
| 파일 작업 API | 실제 원본 생성·수정·삭제·복사·이동 확인 후 색인 일치, 복사 새 ID·동일 source 이동 ID 유지 |
| 파일 작업 충돌/실패 | 버전 충돌·이름 충돌·중복 요청·원본 성공/색인 실패·작업 중단을 구분, 미지원 작업을 성공으로 표시하지 않음 |
| 최종 검색 | OpenViking/LLM route 0, RRF60·문서당8·C128·prefix2400·Jina v3.5·Top5 확인 |
| 평문 보관 | Windows 출력·Docker 저장/작업·MinIO 원본/썸네일/청크 이미지·파서 파생물에 지속 보관 없음; 삭제 실패는 표시 |
| 검색 중 원문 미리보기 검증 | 제7.1.3절의 가능/조건부/불가 판정과 실행 근거 기록. 가능 시 결과 클릭 때만 복호화하고 권한·버전·임시파일 정리 검증; 실패해도 검색/청크 표시 유지, 영구 사본으로 자동 전환 없음 |
| 이전 대화 | 본인 질문·답변·인용을 30일 조회·검색, 만료 즉시 모든 조회/문맥 경로에서 제외, 다른 사용자 기록 미노출, 삭제 원문 링크 접근 차단 |
| 복구 | 별도 환경에서 사전 앱·데이터를 복원해 기준 문서와 검색 확인 |

같은 하드웨어·문서·질의·동시성·캐시 조건에서 검색 p50/p95, 단계별 시간, 오류, 토큰을 비교한다. 수집/복호화/색인 지연은 검색 응답 시간과 별도 기록한다. 과거 외부 API 단일 실행 시간을 운영 SLA로 사용하지 않는다. 목표 문서량·최대 파일 크기·동시 사용자와 숫자 SLA는 배포 전에 정한다.

## 13. 복구와 남은 결정

GitHub 복구 기준은 `pre-openviking-refactor-20260921T061958Z`이다.
https://github.com/Dongmin103/docmind-recovery/releases/tag/pre-openviking-refactor-20260921T061958Z

현재 백업은 원본 Git 이력·미커밋 소스·설정·실행 소스/패키지 복구용이다. DB, ES, 문서 객체 저장소, OpenViking 영속 데이터, Docker 이미지 레이어의 시점 백업은 포함하지 않는다. 마이그레이션 전 이 데이터/이미지 복구 수단을 별도로 확보한다. 전환 후 신규 문서 작업도 롤백 시 잃지 않도록 기록한다.

### 13.1 지금 구체화할 운영·제품 결정

| 항목 | 정할 내용 | 현재 상태 |
|---|---|---|
| 배치 위치 | Windows 장비 쪽 Docker + Windows 호스트 복호화 실행부 | 확정. Docker 환경과 호스트/컨테이너 통신은 구현 시 검증 |
| 초기 접근 범위 | 등록된 세 source 전체를 초기 공용 범위로 사용 | 확정. 로그인/source 경계 유지. 사용자·부서별 축소와 세부 계정/ACL 매핑은 후속. `_DEPT_1` 표시명은 미확인 |
| 시험 규모·성능 목표 | 문서 수·용량·형식·최대 크기, 동시 사용자, 검색/색인/미리보기 허용 대기 시간 | 사용자가 추후 지정. 다른 구현을 막지 않음. 현재 파서를 지원 형식 확정 없이 삭제하지 않음 |
| 쓰기 기능의 공개 순서 | 조회·검색·동기화를 먼저 검증하고 생성·수정·삭제·복사·이동은 실제 원본 반영을 검증한 기능부터 공개 | 사용자 위임에 따라 이 순서로 확정. 쓰기 기능은 전체 구현 목표에 유지 |
| 대화 기록 보관 | 본인의 질문·LLM 답변·인용 근거를 30일간 조회·검색 | 확정. 제9.4절 적용. 운영 작업 로그의 보관 기간은 이 결정과 별개로 운영 전에 정함 |

### 13.2 구현하면서 확인할 기술 항목

- 수집용 CLI: 실행 계정·로그인/로그아웃·재부팅·키/대화 입력·결과 판정·임시 평문 정리. 실제 성공 조건은 제4절 비민감 표본 시험으로 확인한다.
- 검색 중 원문 보기: 제7.1.3절의 클릭 기반 임시 복호화·전달·렌더링·삭제를 시험하고 가능/조건부/불가와 지연을 기록한다. 이것은 이미 구현 중 검증 과제로 선정됐으며 다시 방식 선정을 요구하지 않는다.
- 원본 작업 API: All-in-One의 생성·수정·삭제·복사·이동 인터페이스, 암호화 저장·권한·이력 반영, 휴지통 의미와 source 간 이동 계약을 확인한다.
- RAGFlow 축소: 실제 파서/모델/커넥터/외부 API 소비자/대기 작업을 조사해 제14절 제거 후보의 사용 여부와 공통 의존을 확인한다. C128·prefix2400·Jina v3.5 계약을 변경하지 않는다.

### 13.3 시험 결과 또는 운영 전 정할 항목

- 청크 이미지의 암호화 보관 제안은 미확정이다. 원문 미리보기 시험과 실제 화면 요구를 확인한 뒤 채택 여부·보관 기간을 정한다. 임시 복호화 실패를 영구 사본 보관 승인으로 해석하지 않는다.
- 운영 문서 반입 전에 저장/전송 암호화 적용 방식, 키 보관·관리 책임, 백업 위치/주기와 허용 데이터 손실·복구 시간 목표를 정하고 복원을 검증한다. 현재 소스 백업만으로 데이터 롤백까지 가능하다고 판단하지 않는다.
- 시험 측정값을 바탕으로 CLI/미리보기 동시성, 재시도·timeout·임시파일 TTL 및 최종 성능 기준을 확정한다. 최초 CLI 동시성 1이라는 기존 안전한 시작값은 유지한다.

이 항목 전체가 확정될 때까지 모든 개발을 멈추는 것은 아니다. 기존 색인과 비민감 표본을 이용한 OpenViking 제거·구성 C·범위 검색·공통 의존 분리는 먼저 진행할 수 있다. 실제 원본 연계와 사용자별 데이터 공개는 해당 배치·권한·실행 검증이 확보된 범위에서 진행한다.

## 14. RAGFlow 기능 축소 범위와 호환성

### 14.1 목적과 조사 상태

DocMind에 필요한 클라우드 문서 수집·동기화, 폴더/문서 선택, 색인, 구성 C 검색, 근거 기반 답변에 집중한다. 범용 RAGFlow 제작 도구와 현재 제품 범위 밖 연동을 제거해 화면·운영·배포 의존성을 줄인다. 기존 청커와 BGE-M3는 이 작업에서 변경하지 않는다. AI 폴더 분류·요약은 후속 구현 범위로 둔다.

2026-09-21 복구 백업의 소스와 컨테이너 설정을 정적으로 조사했다. Python 662개 파일의 AST 파싱 오류는 0건이다. 운영 DB의 사용 설정·접근 로그·대기 작업과 백업 이후 변경은 미확인이며, 실행 호환성 검증은 아직 하지 않았다. 다음 표는 제품 범위에 따른 제거 계획으로, 모든 항목의 운영 미사용이 입증됐다는 의미는 아니다.

상세 근거: [RAGFlow 기능 축소 조사 보고서](docs/RAGFLOW-TRIM-AUDIT.md). 조사본은 백업에서 추출한 검토용 코드이며 운영 checkout이 아니다.

### 14.2 제거·대체·유지 목록

| 기능 | 계획 | 삭제 조건 및 유지할 부분 |
|---|---|---|
| OpenViking route/catalog/generation/publish | 제품에서 제거 | source/폴더/문서 메타데이터와 공통 scope로 대체. 평가·복구 자료 보존 |
| 에이전트 빌더·템플릿·도구·코드 실행 | 제거 | 문서 파이프라인이 재사용하는 agent 공통 클래스와 LLM 호출부터 분리 |
| 장기 메모리 | 제거 | 엔진 초기화·API·워커 작업 의존 정리. 일반 대화 이력은 별도 유지 정책 적용 |
| GraphRAG·RAPTOR·마인드맵·위키/편찬·타임라인 | 제거 | 공유 캐시/호출 제한 함수 보존. 기존 설정·작업·파생 청크 처리 후 제거 |
| 외부 데이터 커넥터 | 제거 | 실제 활성 연결/스케줄 확인. 새 All-in-One 원본 감지·자정 재조정은 유지 |
| MCP·플러그인·외부 채팅 채널 | 제거 후보 | 소비자와 공통 DialogService 의존 확인 후 서버/API/라이브러리 정리 |
| 별도 검색·채팅·파일 관리·챗봇 제작 화면 | DocMind 화면으로 통합 | 질문/답변·근거·문서 선택·작업 상태·필요한 이력 유지 |
| 범용 데이터셋/파이프라인 편집 UI | 축소 | Document/Knowledgebase/File/Task 서비스와 필요한 운영자 파서·모델·재처리 기능 유지 |
| 원본 업로드·다운로드·저장형 미리보기 | 클라우드 작업/경로로 대체 | 제9절 원본 작업 API, 사용자 논리 경로, 근거 청크 표시로 연결 |
| MinIO/S3 영구 평문 원본·썸네일·청크 이미지 | 저장 경로 제거 | 파서의 임시 입력·파생물·실패/중단 정리 완성 후 서비스 제거 여부 결정 |
| 웹 검색·DeepResearch·Text-to-SQL·TTS | 미사용 확인 후 제거 | 답변 생성·스트리밍·인용·토큰/오류 처리 유지 |
| 사용하지 않는 모델 공급자·대체 DB/저장소 | 실제 설정에 따라 축소 | Jina v3.5·BGE-M3·현재 답변 LLM·사용 중 OCR/VLM·MySQL·ES 유지 |
| Go API/ingestor·특수 파서·편집 라이브러리 | 후순위 검토 | Docker 빌드·백엔드 판별·문서별 파서 바인딩 확인 후 제거 |

유지할 핵심은 로그인/사용자/tenant/source 권한, 경로·버전 매핑, 문서 파싱/OCR·청킹, BGE-M3, MySQL, Elasticsearch, 작업 큐·락·재시도, 구성 C, 답변·인용, 최소 운영 관리와 오류 관측이다. 백업에 실행 중인 HWP·Docling Office/PDF·Surya 파서는 형식별 회귀 없이 없애지 않는다. Redis 호환 Valkey는 작업 처리 의존성이므로 제거 대상에서 제외한다.

### 14.3 정적 조사에서 확인한 주의 지점

- API 앱은 파일 탐색과 모듈 실행으로 자동 등록된다. 메뉴만 지우면 API와 라이브러리 의존은 남는다. 직접 URL·SDK·과거 호환 라우트까지 확인한다.
- `rag.flow`가 `agent.component`와 `agent.canvas.Graph`를 사용한다. agent 폴더 일괄 삭제 전에 필수 공통 코드를 분리한다.
- 일반 작업 워커가 메모리/RAPTOR 코드를 import하고 GraphRAG의 캐시·호출 제한을 공유한다. 기능 분기와 공통 유틸을 함께 정리한다.
- `common/settings.py`는 여러 DB/저장소 드라이버를 import하고 메모리 저장 연결·KG 검색기를 초기화한다. 설정/팩토리/초기화와 패키지 의존을 함께 축소한다.
- 현재 등록 경로가 FileService/DocumentService를 호출하고 파서는 객체 저장소에서 원본을 읽는다. MinIO를 먼저 중지하지 않고 제7절 임시 처리 계약으로 바꾼다.
- DocMind 화면이 기존 문서 미리보기와 데이터셋 청크 타입 등을 가져온다. 화면 통합 전 공통 컴포넌트를 분리한다.
- 백업 당시 Python API·MySQL·ES와 shared workspace 설정이 확인됐다. 새 개인 home/부서 권한을 공용 사용자 매핑으로 대체하지 않는다. Go 경로는 실제 ingestor·빌드 사용까지 확인한다.

### 14.4 삭제 전 확인 및 구현 순서

1. 실제 실행 이미지/checkout과 조사본의 차이, DB의 파서·파이프라인·모델·그래프 설정, 커넥터/스케줄, 큐 작업 종류, 외부 API 호출자를 읽기 전용으로 확인한다. 비밀 값은 출력하지 않는다.
2. 제13절의 소스 복구 기준에 더해 데이터/이미지 복원 수단을 확보하고 격리 환경에서 작업한다. 기존 DB/ES/객체가 현재 소스 백업에 포함된 것으로 간주하지 않는다.
3. 제11절 OpenViking 제거와 C 검색 전환을 먼저 검증한다. 이후 기능 묶음별로 UI/API/작업 생성 진입점→공통 의존 분리→워커/서비스 코드→패키지·lockfile·Docker 순서로 정리한다.
4. 폐기 기능의 기존 큐 작업은 완료/취소/변환 정책을 정하고 처리한다. 제거한 작업을 성공으로 위장하거나 무한 재시도하지 않는다. 호환 래퍼는 실제 외부 계약이 있을 때만 명시적인 이전 기간과 함께 사용한다.
5. 영구 평문 저장은 임시 입력·파생물 정리가 준비된 뒤 제거한다. 문서별 사용 파서와 모델이 검증되기 전에는 전문 파서/공급자를 일괄 삭제하지 않는다.
6. 기능 회귀와 복원 검증 후 제한적으로 전환한다. 테이블·색인·객체의 물리 삭제는 별도 변경으로 분리하고 목록/수량 dry-run, 보존 정책, 복원 가능성을 확인한다. 기능 묶음별 커밋과 데이터 변경 기록을 남긴다.

### 14.5 호환성 합격 기준

- 신규 DB와 기존 DB 업그레이드 모두 API/worker 기동·설정·마이그레이션이 성공하고 삭제 모듈의 import가 남지 않는다.
- 프런트 build/typecheck/lint와 필요한 경로 검증을 통과한다. 삭제 기능의 메뉴·직접 URL·API 진입점과 새 작업 생성 경로가 남지 않는다.
- PDF/Office/HWP/스캔 표본의 파싱·청크·색인, 재처리·실패·재시작 복구를 검증한다. 기존 청커/BGE-M3의 결과를 의도 없이 바꾸지 않는다.
- 전체/폴더 하위/선택 문서에서 제10절 C128·prefix2400·Jina v3.5·Top5를 확인하고, 권한/활성 버전을 두 lane 후보 생성 전에 적용한다.
- 답변 스트리밍·취소·인용·필요한 이력·모델 오류 처리가 유지된다. 원문 접근은 사용자 논리 경로와 권한을 따른다.
- 제12절 원본 생성/수정/삭제/복사/이동·중복 이벤트·자정 정합성·평문 정리 기준을 모두 유지한다.
- 같은 조건에서 기동 시간·메모리·이미지 크기와 검색 p50/p95·오류·Jina usage를 비교한다. 삭제량만으로 속도 개선을 가정하지 않는다.
- 소스·이미지·DB·ES·필요한 원본 상태 복원과 전환 후 작업 이력 대조를 확인한다. 정적 분석 통과를 실제 호환성 통과로 표시하지 않는다.

## 자료와 결정 이력

- 최신 사용자 설명: All-in-One DC 업로드→서버 실제 폴더의 암호화 원본→임시 복호화→색인→평문 삭제, 원본과 색인 동기화.
- 복호화 실행 사진은 로컬 조사 자료이며 이 개발 저장소에 포함하지 않는다. 관찰 사실은 제4절에 기록했다.
- 경로 매핑 사진은 포함하지 않는다. 확인된 세 루트와 표시 경로는 제2절을 따른다.
- 기존 복호화 정적 조사 원문은 로컬 자료다. 공급사 미지원 답변과 미검증 실행 조건은 본 PRD에 반영했다.
- C128 정의는 제10절 및 [C 검색 이식 계약](docs/C-SEARCH-CONTRACT.md)을 따른다. 과거 평가 원문·청크·결과 데이터는 포함하지 않는다.
- 기존 복구 이력: 저장소 루트의 RESTORE.md, checkpoint.json, VERIFICATION.json 및 기존 암호화 복구 릴리스. 새 개발에는 해당 백업·키를 내려받지 않는다.
- 과거 수동 수집 제안과 공급사 질의 문서는 현행 설계가 아니다. 이 저장소에서는 본 PRD를 기준으로 개발한다.
